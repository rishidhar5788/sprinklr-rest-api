source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in sam.gemspec
gem 'dotenv-rails', groups: [:development, :test]
gemspec
